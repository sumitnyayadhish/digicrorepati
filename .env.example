# Copy to .env for local dev. On Vercel, set these in Project → Settings → Environment Variables.
# Neon Postgres connection string (auto-added by the Vercel Neon integration)
DATABASE_URL=
SITE_URL=https://digicrorepati.vercel.app
CONTACT_EMAIL=
# Any long random string — lets you download collected leads at /api/lead?key=ADMIN_KEY
ADMIN_KEY=
PORT=3000
